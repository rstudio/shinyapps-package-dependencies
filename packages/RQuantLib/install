#!/bin/bash
set -x
set -e

apt-get -qq update
apt-get -y --force-yes install libboost-all-dev

VERSION=1.4
wget http://softlayer-dal.dl.sourceforge.net/project/quantlib/QuantLib/$VERSION/QuantLib-$VERSION.tar.gz
tar -zxvf QuantLib-$VERSION.tar.gz
cd QuantLib-$VERSION

./configure
make
make install
